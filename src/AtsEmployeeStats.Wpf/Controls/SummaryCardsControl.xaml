<UserControl x:Class="AtsEmployeeStats.Wpf.Controls.SummaryCardsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ItemsControl ItemsSource="{Binding SummaryCards}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Background="#FFFFFF"
                        BorderBrush="#CBD5E1"
                        BorderThickness="1"
                        Padding="12,9"
                        Margin="0,0,10,10"
                        MinWidth="150">
                    <StackPanel>
                        <TextBlock Text="{Binding Value}"
                                   Foreground="#111827"
                                   FontSize="17"
                                   FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding Label}"
                                   Foreground="#475569"
                                   FontSize="12" />
                        <TextBlock Text="{Binding Detail}"
                                   Foreground="#94A3B8"
                                   FontSize="11"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
